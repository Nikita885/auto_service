<?xml version="1.0" encoding="utf-8"?>
<!--
  По умолчанию открытый http запрещён: по каналу ходит JWT и телефон клиента.
  Исключение одно — адрес эмулятора в отладочной сборке, иначе разработчику
  пришлось бы поднимать TLS ради локального сервера.

  Перед публикацией: проверьте, что домен в BuildConfig.API_BASE_URL — https.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
