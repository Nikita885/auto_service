name: autoservice

# Логи Docker по умолчанию растут без предела: json-file пишет, пока не
# кончится диск. На сервере с одним небольшим томом это отказ сайта —
# PostgreSQL первым получит «no space left on device». Ограничение общее
# для всех сервисов, поэтому вынесено в якорь.
x-logging: &logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"

# api, worker и beat — это один и тот же код с разной командой запуска.
# Общий якорь гарантирует, что образ собирается один раз и версии кода
# в веб-процессе и в фоновых задачах не разъезжаются.
x-app: &app
  build:
    context: .
    args:
      REQUIREMENTS: dev
  image: autoservice-api:latest
  env_file: .env
  restart: unless-stopped
  logging: *logging
  volumes:
    - .:/app
    # Именованные тома поверх bind-mount: каталоги принадлежат пользователю
    # контейнера, иначе collectstatic и загрузки упираются в права хоста.
    - static:/app/static
    - media:/app/media

services:
  db:
    image: postgres:16-alpine
    # Без политики перезапуска база не поднимается после перезагрузки
    # сервера, а api с `unless-stopped` уходит в бесконечный рестарт —
    # сайт лежит до ручного вмешательства.
    restart: unless-stopped
    logging: *logging
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-autoservice}
      POSTGRES_USER: ${POSTGRES_USER:-autoservice}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-autoservice}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-autoservice}"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    logging: *logging
    command: redis-server --appendonly yes
    volumes:
      - redisdata:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    <<: *app
    command: api
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    <<: *app
    command: worker
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  beat:
    <<: *app
    command: beat
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  pgdata:
  redisdata:
  static:
  media:
